// Create a Context – Use createContext().

// Provide the Context – Wrap components with a Provider.

// Consume the Context – Use useContext() in child components.


import React from 'react'

const UserContext = React.createContext();

export default  UserContext;

